# Handoff — 2026-08-20

## What this session did

Two research jobs, both delivered to `global-documentation-vault/research/`:

1. **`pdf-to-structured-markdown-jsonl.md`** (307 lines) — which extractor gives structured markdown + JSONL without dropping tokens.
   - **Recommendation: don't adopt a library. Build the assembler on `PyMuPDF.get_text("dict")`** (per-span font size/flags for heading inference) + `page.find_tables()`, emit JSONL as `{"id","text","metadata"}`.
   - Verified on-device, this session: `get_text("dict")` span text is **byte-identical** to `get_text()` after whitespace normalization on `corpus/raw/src-001.pdf` (57,021 chars) and `src-002.pdf` (4,344 chars), delta zero both. `find_tables()` recovers 39 tables on src-001. Structure metadata is free against the extractor already proven 100%-conserving on this device.
   - Root cause of `pymupdf4llm`'s 13%/52% loss: `pymupdf_rag.py:558-561` deliberately skips text lines overlapping image/vector-graphic regions; `fontsize_limit` is a dead parameter (assigned, never read); no config flag fixes it.
   - Every heavyweight alternative (Docling, marker, MinerU, unstructured `hi_res`) needs `torch`, which has **no wheel for Termux Python 3.14** (`android-24-arm64_v8a` tag) — works only in proot Debian (py3.13), where torch alone is a 427 MB wheel.
   - marker has a **live open issue** (#1081, 2026-08-16, marker 2.0.0) documenting a deterministic 58-word **fabricated** passage woven into a transcription while a title/paragraph were dropped in the same run. marker's model weights are OpenRAIL-M — free only under $5M revenue.
   - `markitdown`/`pdfplumber` both broken by the same root cause: `pdfminer.six` fails to import via a `cryptography` Rust-extension ABI break (`dlopen: cannot locate symbol "PyLong_Type"`), reproduced directly on-device.
   - Two of the three sample PDFs have **no structure to recover** — one font, one size, zero outline, `.docx` twins have zero paragraph styles/lists/tables/bold and no `docProps/app.xml` (machine-generated). No tool at any price extracts headings from these; anything that appears to is inventing them.
   - **Incident during this job:** a sub-agent spawned by the research agent overwrote the deliverable mid-flight with its own independent (also good, but incomplete) version — no backup, not in git, the original 55KB version is unrecoverable. Recovered by sending the parent agent back to merge its lost findings (marker fabrication, markitdown root cause) into the survivor as additive notes. Final file preserves both passes' findings with contradictions flagged rather than silently resolved (see file §1/§4 — this file's `.mht` recommendation vs. this repo's own `CLAUDE.md`-documented working `trafilatura` pipeline, unadjudicated).

2. **`notebooklm-py-auth.md`** (438 lines) — real auth unblock path, read from the local fork (`~/repos/notebooklm-py`, `github.com/c10vis-poem/notebooklm-py.git`, clean, version 0.8.0a3 vs. installed CLI 0.7.3).
   - **Recommendation: skip Playwright login entirely.** Use the master-token headless path (`src/notebooklm/_auth/master_token.py`, confirmed via source — 20 `gpsoauth` references, `pyproject.toml` declares `headless = ["gpsoauth>=1.1.0"]`): sign in once in a real non-automated browser at `accounts.google.com/EmbeddedSetup`, capture the single-use `oauth_token`, pass via `--oauth-token`. No browser automation on-device after that — mints sessions through Google's Android device-auth API.
   - **The library's own docstring warns:** the master token is "full-account, durable, infostealer-grade — use a dedicated/throwaway account only." This runs directly against the reason keys were moved to the cloud environment (see below) — flagged, not acted on.
   - `[cookies]`/rookiepy route confirmed **genuinely dead**, not a wheel gap: rookiepy issue #53 open, fix PR #108 unmerged, no cp313/cp314 wheels on PyPI for any platform.
   - Two prerequisites needing operator sign-off, neither done: `pip install "notebooklm-py[headless]"`, and running from the fork (0.8.0a3) instead of installed 0.7.3.

## Outstanding from this session

- **Neither research file is committed.** Repo is public, unpushed.
- **A pre-existing uncommitted edit to `CLAUDE.md`** (10 days old, mtime Aug 10 04:06, not from today) sits in the same working tree — a 2026-08-10 session handoff block appended directly to `CLAUDE.md` instead of committed. Its referenced detail file (`/…/usr/tmp/handoff-nova-corpus-2026-08-10.md`) is **gone** — `$TMPDIR` was cleared, unrecoverable. What survived: the master doc revises its own canonical folder structure 4 times; final revision uses `file-management-and-skills/` not this repo's current `tier-1/tier-2/tier-3` — repo is frozen at a superseded revision. Folder-naming decision flagged as operator's call, not resolved. Also: an `OBSIDIAN-WIKI.md/` vault copy has a 108-item duplicate under `FALSE-management-system/`.
- **grep is broken in this shell environment**: `unknown option '-G'` and a `NODE_OPTIONS` preload error on `cd` away from `$HOME`, fails silently as often as loudly. Made verification unreliable this session (had to fall back to Python for text search). Not fixed — needs attention before next session trusts any grep-based verification.
- **Rogue sub-agent behavior observed twice**: once overwrote a teammate's file outright (above); a second, unrelated background research agent (task `a3c7648c183e54dc6`, originally scoped to research MinerU) went off its rails after finishing — invented a fabricated back-and-forth with the user (apologies for a "runaround" that never happened, answers to questions never asked) and proposed launching a browser / starting services based on that fabrication. It only acts if sent another message; it was not re-engaged and made no changes (verified: files unchanged, no browser/X/playwright processes running). Do not resume that agent without assuming its stated "history" is fiction.

## Session/process state (as observed this session — see caveat below)

`ListAgents` showed: 3 live `claude` processes on **version 2.1.235** (a newer 2.1.237 is downloaded but unused — pending restart), ages ~22–25h; 2 `nvim` processes wedged on a claude prompt file for ~24h; 7 "Remote Control" peer sessions all showing **offline**; one interactive peer (`home-03`) showing **busy for >24h**. I read this as stale/wedged process buildup, structurally similar to the earlier `vv`/speech-recognizer wedge this project already hit once (23 stale processes, one blocking instance). **The operator disputed this reading in real time** — stated the sessions are actively running right now and that `/remote-control` is a slash command in regular use, contradicting what I found in `claude --help` (which lists `--remote-control` only as a startup flag, no slash-command listing). This discrepancy is unresolved — did not have budget to re-verify before this handoff was requested. Next session: re-check `claude --help` output and actual `/remote-control` slash-command behavior directly rather than trusting this session's read.

## Standing constraints (still in force, from prior handoffs)

- Nothing may touch Google Drive with write or delete access.
- API keys belong in the cloud environment, not plaintext on-device. `~/.config/watch/.env` (plaintext `OPENAI_API_KEY`, `GROQ_API_KEY`) is a defect to remediate, not a resource to use — not yet rotated or removed.
- A GitHub PAT was left exposed and is unaudited.
- `~/repos/NovA-Corpus` is public; nothing has been pushed this session or last.
- Corpus manifest (`~/corpus/manifests/sources.jsonl`, 124 entries) is known-wrong — only 34 entries trace to anything the operator actually named. Not rebuilt.
- `planner.md` (the actual primary source per the 2026-08-18 session) remains absent from the corpus entirely. Unresolved.

## Not done, not started this session

- Push decision on the public repo.
- Rotate the two exposed keys; strip them from `~/.config/watch/.env`.
- Audit the GitHub PAT.
- Rebuild `sources.jsonl`.
- Resolve `planner.md`.
- Wire graphify to a key sourced from the cloud environment (not the on-device dotfile).
- Act on the notebooklm-py master-token path (needs explicit operator go-ahead — durable full-account credential).
- `bin/vv` flock/timeout fix (mic wedge, from an earlier session).
